VaultWatch — production-grade DeFi risk intelligence on Casper. Six AI agents. Eight deployed smart contracts. Every finding written permanently on-chain.

The Risk Intelligence engine accepts natural language queries. Ask about a liquidity pool, a protocol, an RWA asset. Six agents — Scanner, Anomaly, SelfCorrection, RWA, Intel, and Audit — activate simultaneously. Within seconds: a structured risk verdict, a confidence score, and a transaction hash proving the finding is now on the Casper blockchain.

The Agent Pipeline Findings show the full picture. CasperSwap: CRITICAL — whale concentration, exit liquidity insufficient for large positions. CasperLend: HIGH — three positions at liquidation boundary, collateral ratio at 1.08x. CasperYield: HIGH — 14x withdrawal spike in two hours, TVL down 22 percent.

The Anomaly Detection module runs AnomalyAgent with a SelfCorrectionAgent feedback loop. Risk score 42 out of 100 — ELEVATED. Two anomalies flagged. The recommendation is written directly to the RiskOracle contract.

The Live Feed shows all six agents active in real time. AuditAgent logging to AuditTrail. x402 payment processed — 0.5 CSPR deducted from SubscriberVault. All eight contracts confirmed ON-CHAIN.

This is the Casper testnet explorer — the deployer account. Public key verified. 3,957 CSPR balance. Activity across three pages. Every transaction is a contract deployment or on-chain interaction — all independently verifiable by any judge at testnet.cspr.live right now.

This is the test suite. One hundred and thirty tests. All passing. Three point four seconds. pytest across unit and integration suites — ScannerAgent, AnomalyAgent, SelfCorrectionAgent, RWAAgent, AuditAgent, IntelAgent, SidecarClient. Then the pipeline starts: seven workers active, all six agents online, writing to Casper in real time.

VaultWatch. Production-ready. On-chain. Live.
